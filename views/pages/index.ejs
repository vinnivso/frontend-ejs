<!DOCTYPE html>
<html lang="en">

  <%- include ("../partials/head"); %>

  <body class="container">
  <!-- É possível passar objetos no EJS, como conteúdo, assim como demonstrado abaixo. -->
  <%- include ("../partials/header"); %>

    <main>
      <div class="bg-light p-5 rounded-lg m-3">
        <h1>Nodejs com EJS</h1>
        <p>Aprendendo a trabalhar com arquivos EJS</p>
        <p><%= subtitle %></p>
      </div>
      <ul>
        <!-- Criando um foreach para renderizar cada elemento dentro do meu objeto criado para INDEX -->
        <% qualities.forEach(function(element) { %>
          <li>
            <strong><%= element.title %></strong><%= element.message %>
          </li>
        <% }) %>
      </ul>
    </main>

  <%- include ("../partials/footer"); %>
  </body>
</html>
